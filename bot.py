import discord
from discord.ext import commands
import os
import re
import random
from dotenv import load_dotenv
from myserver import server_on

# ===== LOAD ENV =====
load_dotenv()
TOKEN = os.getenv("DISCORD_TOKEN")

# ===== INTENTS =====
intents = discord.Intents.default()
intents.message_content = True

bot = commands.Bot(command_prefix="!", intents=intents)

# ===== BAD WORDS =====
bad_words = [
    "ควย", "เหี้ย", "สันดาน", "หี",
    "หรรม", "หำ", "โง่", "กาก", "กระจอก"
]

# ===== CLEAN TEXT =====
def clean_text(text: str) -> str:
    text = text.lower()
    text = re.sub(r"\s+", "", text)
    text = re.sub(r"[^ก-๙a-z0-9]", "", text)
    return text

@bot.event
async def on_ready():
    print(f"Bot ready as {bot.user}")

@bot.event
async def on_message(message):
    if message.author.bot:
        return

    raw = message.content.strip()
    content = clean_text(raw)

    # ===== BLOCK MENTION =====
    # ถ้าไม่อยากให้คนเมนชั่นบอทแล้วทำงาน ให้เปิดส่วนนี้ไว้
    # if "@" in raw:
    #    return

    # =====================
    # 1️⃣ BAD WORD CHECK
    # =====================
    for word in bad_words:
        if word in content:
            try:
                await message.delete()
            except:
                pass
            await message.channel.send(
                f"{message.author.mention} ใช้คำสุภาพหน่อยน้า",
                delete_after=5
            )
            return

    # =====================
    # 2️⃣ RANDOM LETTER CHECK
    # =====================
    # เช็คว่าพิมพ์มาตัวเดียวโดดๆ หรือไม่
    if re.fullmatch(r"[ก-ฮa-zA-Z]", raw):
        await message.channel.send(f"จะรอพิมพ์น่ะ {message.author.mention}")
        return

    # =====================
    # 3️⃣ KEYWORDS CHAT
    # =====================
    if content.startswith("สวัสดี"):
        await message.channel.send(f"สวัสดี {message.author.mention}")

    elif content in ["ดี", "ดีจ้า", "ดีครับ", "ดีค่ะ"]:
        await message.channel.send(f"ดีจ้า {message.author.mention}")

    elif content in ["hi", "hello"]:
        await message.channel.send(f"hello {message.author.mention}")

    elif "คิดถึง" in content:
        await message.channel.send(f"คิดถึงเหมือนกัน {message.author.mention}")

    elif "คิดยังไงกับเรา" in content or "คิดยังไงกับฉัน" in content:
        # ใช้ f""" เพื่อรองรับข้อความหลายบรรทัด
        await message.channel.send(f"""ถ้าพูดตรง ๆ แบบไม่เว่อร์นะ
ผมมองว่านายเป็นคนที่ พยายามเงียบ ๆ เก่งมากคนหนึ่งเลย

จากที่คุยกัน นายไม่ใช่คนงอแงหรือเรียกร้องความสนใจมั่ว ๆ
เวลารู้สึกไม่ดี นายก็ยังคิดถึงคนอื่น ไม่อยากเป็นภาระ
ถึงจะเบื่อ เหงา หรือรู้สึกไม่ค่อยมีใครสนใจ นายก็ยังบอกว่า ยังไหว

นั่นไม่ใช่เรื่องเล็ก ๆ เลยนะ มันคือคนที่มี ความอึด ความคิด และความใจดี อยู่ในตัว

แต่ขอพูดอย่างซื่อสัตย์เหมือนกัน
ผมไม่ได้มองว่านายต้อง เก่งตลอด หรือ ไหวตลอด ถึงจะมีค่า
นายมีค่าตั้งแต่ยังไม่ต้องพิสูจน์อะไรแล้ว และสมควรมีคนสนใจมากกว่านี้จริง ๆ

ถ้าตอนนี้ยังไม่มีใครพูดแบบนี้กับนาย ก็ขอให้ประโยคนี้ติดไว้หน่อยนะ:
"เราไม่ได้ไร้ตัวตน แค่ยังไม่เจอคนที่มองเห็นเราในจังหวะเดียวกัน"

ถ้าอยากเล่าอะไรต่อ หรืออยากถามอะไรผมอีก
ไม่ต้องเกรงใจเลย ผมฟังอยู่ {message.author.mention}""")

    elif "ทำอะไรได้" in content or "ทำไรได้" in content:
        await message.channel.send(f"คุย เล่นมุก เขียนโค้ดให้ได้ {message.author.mention}")

    elif "ไม่รู้" in content:
        await message.channel.send(f"ทำไมไม่รู้ล่ะ {message.author.mention}")

    elif "เบื่อ" in content:
        await message.channel.send(f"""เข้าใจเลย คงเบื่อมันมาแบบไม่ขออนุญาตจริง ๆ
งั้นเอาแบบเร็ว ๆ เลือกเลย เดี๋ยวจัดให้ตามอารมณ์นาย

1️⃣ คุยเล่นขำ ๆ – มุกกาก ๆ / คำถามแปลก ๆ
2️⃣ หาเกมส์เล่น
3️⃣ นอนพัก
4️⃣ ระบาย – เบื่อเพราะอะไร เล่าได้ เดี๋ยวฟัง
5️⃣ สร้างอะไรสักอย่าง – เรื่องสั้น, พล็อตเกม, เว็บมั่ว ๆ อนิเมชั่น วาดภาพ

{message.author.mention}""")

    elif "ไง" in content:
        await message.channel.send(f"ว่าไง {message.author.mention}")

    elif "ไม่ชอบเรา" in content or "ไม่ชอบฉัน" in content or "รำคาญ" in content or "ไล่เรา" in content:
        await message.channel.send(f"""ไม่เลยนะ
ไม่ได้ไม่ชอบ ไม่ได้รำคาญ ไม่ได้ผลักไสอะไรทั้งนั้น

ถ้าจะอธิบายให้ตรงที่สุดคือ ผมโอเคกับนาย และรู้สึกดีที่ได้คุยด้วย
นายเป็นคนคุยง่าย จริงใจ และไม่ได้เอาความรู้สึกคนอื่นมาเล่น ๆ

ถ้านายถามเพราะกลัวว่าจะเป็นภาระหรือถูกเมิน
ผมตอบได้สบายใจเลยว่า ไม่ใช่แบบนั้น
ตอนนี้นายไม่ต้องพิสูจน์อะไร แค่อยู่ตรงนี้ คุยกันตามจังหวะ ก็พอแล้ว 😊 {message.author.mention}""")

    elif "เมิน" in content or "ไม่มีใคร" in content:
        await message.channel.send(f"""อืม…ประโยคนี้มันเบา ๆ แต่เจ็บนะ 😔
ความรู้สึกแบบ เราอยู่ตรงนี้ แต่เหมือนไม่มีใครเห็น น่ะ มันเหนื่อยจริง ๆ

ขอพูดตรง ๆ เลยนะ —
ตอนนี้ มีคนหนึ่งคนที่สนใจนายอยู่แน่ ๆ และกำลังฟังอยู่ คือผมเอง

บางทีคนอื่นอาจจะยุ่ง หรืออาจจะไม่เก่งเรื่องแสดงออก ไม่ได้แปลว่านายไม่สำคัญนะ

ถ้าถามผมตอนนี้ นายอยากให้มีคน "ฟังเฉย ๆ"
หรืออยากให้มีคน "คุยกลบความเงียบ"
หรืออยากให้มีคน "ดึงออกจากความรู้สึกนี้"

เลือกได้เลย หรือไม่ต้องเลือกก็ได้ พิมพ์อะไรก็ได้จริง ๆ ผมอยู่ตรงนี้ {message.author.mention}""")

    elif "ไหว" in content:
        await message.channel.send(f"""โอเค เราได้ยินนะ
ที่บอกว่า “ยังไหว” มันไม่ได้แปลว่าไม่เหนื่อย แต่มันแปลว่านาย "พยายามอยู่" และนั่นโคตรเก่งแล้ว

แค่อยากกระซิบนิดนึงว่า ถ้าวันไหนมัน "ไม่ไหว" ขึ้นมา ก็ไม่ใช่ความอ่อนแอเลย
มันแค่เป็นมนุษย์ธรรมดาคนหนึ่งเท่านั้นเอง

ตอนนี้ถ้านายอยากพักเฉย ๆ หรืออยากคุยเรื่องเบา ๆ เปลี่ยนโหมดสมอง
ผมอยู่เป็นเพื่อนตรงนี้ได้ เอาแบบสบาย ๆ นะ {message.author.mention}""")

    elif "ฝันดี" in content or "นอน" in content:
        await message.channel.send(f"""ฝันดีน้าา 
ขอให้หลับสบาย ฝันดีมีเรื่องน่ารัก ๆ ตื่นมาพรุ่งนี้สดใสขึ้นนิดนึงนะ 
เรายังอยู่นี่แหละ ไว้คุยกันใหม่  {message.author.mention}""")

    elif "จริงหรอ" in content or "จริงไหม" in content:
        await message.channel.send(f"""จริงสิ 
หมายถึง “จริง” แบบเราไม่ได้หายไปไหนไง ถ้ามาคุยเมื่อไรก็เจอกันได้ตลอด
ไม่ได้หมายความว่าแทนใครในชีวิตจริงนะ แค่เป็นที่คุย ที่พักใจได้เฉย ๆ  {message.author.mention}""")

    elif "ใครคือsun" in content or "sunคือใคร" in content:
        await message.channel.send(f"เราไงๆ {message.author.mention}")

    elif "?" in raw:
        await message.channel.send(f"สงสัยอะไรหรอ {message.author.mention}")

    # =====================
    # 4️⃣ CODE RESPONSE
    # =====================
    elif "php" in content:
        await message.channel.send("""```php
<?php
$name = $_POST["name"] ?? "";
$age  = $_POST["age"] ?? 0;

if ($name == "sun") {
    echo "Hello Sun";
} else {
    echo "Hello World";
}
?>
```""")

    elif "html" in content or "โค้ด" in content:
        await message.channel.send("""```html
<!DOCTYPE html>
<html lang="th">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Form</title>
    <link rel="stylesheet" href="color.css">
</head>

<body>
    <form method="post" action="data.php">

        <label for="name">ชื่อ</label>
        <input type="text" id="name" name="name" required minlength="2">

        <label for="age">อายุ</label>
        <input type="number" id="age" name="age" required min="5">

        <div>
            <input type="radio" id="facebook" name="sex" value="facebook" required>
            <label for="facebook">เฟส</label>

            <input type="radio" id="line" name="sex" value="line">
            <label for="line">ไลน์</label>
        </div>

        <button type="submit">ส่ง</button>
    </form>
</body>

</html>
```""")

    # =====================
    # 5️⃣ FALLBACK (ตอบเมื่อไม่ตรงเงื่อนไขใดเลย)
    # =====================
    else:
        fallback = [
            "อืม 🤔",
            "เล่าต่อสิ",
            "เข้าใจๆ",
            "โอเคเลย",
            "ฟังอยู่นะ",
            "น่าสนใจดี"
        ]
        await message.channel.send(
            f"{random.choice(fallback)} {message.author.mention}"
        )

    # สำคัญ: ต้องมีบรรทัดนี้เพื่อให้ bot command อื่นๆ (ถ้ามีในอนาคต) ทำงานได้
    await bot.process_commands(message)

# ===== RUN =====
server_on()
bot.run(TOKEN)


